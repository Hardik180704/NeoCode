{
  "name": "neocode",
  "version": "0.2.0",
  "private": true,
  "packageManager": "bun@1.3.13",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun run --hot  packages/server/src/index.ts",
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "dev:web": "bun run --cwd packages/web dev",
    "build:cli": "bun run --filter @neocode/cli build",
    "build:web": "bun run --cwd packages/web build",
    "db:generate": "bun run --cwd packages/database db:generate",
    "db:push": "bun run --cwd packages/database db:push",
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/database/tsconfig.json && tsc --noEmit -p packages/server/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json && tsc -b packages/web/tsconfig.json",
    "lint": "biome lint .",
    "format": "biome format --write --staged",
    "check": "bun run lint && bun run typecheck && bun test && bun run build:web",
    "link:cli": "bun run build:cli && cd packages/cli && bun link",
    "release:build": "bun scripts/build-release.ts",
    "release:smoke": "bun scripts/smoke-release.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "typescript": "7.0.2"
  }
}
